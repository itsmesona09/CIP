function signOut() {
    // Logic for signing out (e.g., redirect to login page)
    alert("Signed out successfully!");
    window.location.href = "teacher.html"; // Redirect to login page
}

window.onload = function() {
    // Additional logic that runs when the page loads can go here
};
